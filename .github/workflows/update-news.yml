name: Update Briefing

on:
  workflow_run:
    workflows: ["Daily Briefing Generation"]
    types:
      - completed
  workflow_dispatch:  # 手動觸發

jobs:
  update-briefing:
    runs-on: ubuntu-latest

    steps:
    - name: Update briefing via API
      run: |
        curl -X POST https://dailyaifinancialbriefing-jhlqmrdejnrup8olu8ly2c.streamlit.app/update
